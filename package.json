{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "devDependencies": {
    "lerna": "^8.0.0",
    "vitepress": "^1.0.0-rc.31"
  },
  "scripts": {
    "lerna": "npx lerna run build",
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview"
  }
}
